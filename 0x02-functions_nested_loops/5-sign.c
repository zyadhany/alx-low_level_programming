#include <stdio.h>
#include <math.h>
#include "main.h"

/**
 * print_sign - start
 * @n: the input to be cheaked
 * Return:0
*/

int print_sign(int n)
{
	if (n > 0)
	{
		return (1);
	} else if (n == 0)
	{
		return (0);
	}

return (-1);
}

